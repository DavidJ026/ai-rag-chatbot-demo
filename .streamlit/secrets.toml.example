# Copy to secrets.toml (gitignored) and fill in.
# Mirror the same keys into the Streamlit Community Cloud secrets UI.
ANTHROPIC_API_KEY = "sk-ant-..."
VOYAGE_API_KEY = "pa-..."
APP_PASSWORD = "choose-something"
